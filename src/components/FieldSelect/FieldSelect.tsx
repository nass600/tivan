import styled from 'styled-components'
import ReactSelect from 'react-select'

export const FieldSelect = styled(ReactSelect)`
    order: 3;
    width: 100%;

    &,
    & * {
        cursor: pointer;
    }
`
