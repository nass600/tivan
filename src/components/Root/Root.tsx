import { createGlobalStyle } from 'styled-components'

const Root = createGlobalStyle`
  #tivan {
    position: absolute;
    width: 100%;
    height: 100%;
    color: white;
  }
`

export default Root
